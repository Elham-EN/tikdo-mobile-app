// Used to define build profiles, submission settings, and workflow
// automation for iOS and Android apps. It allows developers to customize
// how eas build and eas submit commands behave, such as setting different
// environments (development, preview, production) without needing manual
// configuration for every build.

{
  // eas.json dictates how the application is built and shipped.
  "build": {
    "development": {
      "developmentClient": true,
      "distribution": "internal"
    },
    // Test out the app in production-like circumstances.
    // In this way, they are similar to production builds.
    // However, they are different from production builds because
    // they are either not signed for distribution on app stores
    "preview": {
      // This tells EAS to create a build that can be installed
      // on real devices without going through the Apple App Store
      // or Google Play Store.
      "distribution": "internal",
      // This sets an environment variable during the build process.
      "env": {
        "APP_VARIANT": "preview"
      },
      // This automatically increases the build number (iOS) or version
      // code (Android) by 1 for every new build. This prevents errors
      // when re-uploading new versions for testing, saving you from
      // having to manually increment numbers.
      "autoIncrement": true,
      // This maps this build to a specific EAS Update channel named "preview".
      "channel": "preview"
    },
    // Defines settings for the final app store release.
    "production": {}
  }
}
